app = "letta-remote"
primary_region = "sjc"

[build]
  dockerfile = "Dockerfile"

[env]
  ENV_NAME = "fly"

[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1

[mounts]
  source = "letta_data"
  destination = "/root"
