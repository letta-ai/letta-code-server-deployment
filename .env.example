# Optional on Letta Cloud. If unset, `letta server` starts OAuth device flow
# and prints an authorization URL in the logs.
LETTA_API_KEY=

ENV_NAME=cloud

# Required for self-hosted deployments.
# LETTA_BASE_URL=http://localhost:8283

# Force file-based token persistence instead of keychain if needed.
# LETTA_SKIP_KEYCHAIN_CHECK=1
